import numpy as np

clean_ds = np.loadtxt("wifi_db/clean_dataset.txt")
noisy_ds = np.loadtxt("wifi_db/noisy_dataset.txt")

#print(noisy_ds)
print(clean_ds)

node = {'attribute', 'value', 'left', 'right'}

def decision_tree_learning(ds, depth):
    if(true):
        print("hi")


def find_split(ds):
    return
